export enum MetricTypes {
  gauge = 'Gauge',
  summary = 'Summary',
  histogram = 'Histogram',
  counter = 'counter',
}

export enum MetricsConstants {
  client = 'module_metrics_client',
  options = 'module_metrics_options',
}
